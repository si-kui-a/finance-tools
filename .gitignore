# 真實財務資料匯出檔——防止誤入公開倉庫(見 hooks/check-no-real-data.sh 同一防護目的)
# 用 /開頭錨定在repo根目錄，不會誤擋 sample-data/ 底下的 .sample.json 範例檔
/ledger.json
/config.json
/estimator_*.json
/wage_reverse_*.json
/loan_*.json

# 2026-08-14發現：使用者自建的記帳工作區資料夾，內容是真實財務資料匯出
# （ledger/currentMonthlySalaryCents等欄位），不在上面既有pattern範圍內
/記帳區/
